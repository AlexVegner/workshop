
import 'package:flutter/material.dart';

class FavoriteAsteroidListPage extends StatelessWidget {
  const FavoriteAsteroidListPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('FavoriteAsteroidListPage')),
    );
  }
}