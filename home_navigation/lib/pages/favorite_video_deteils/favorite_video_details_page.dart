
import 'package:flutter/material.dart';

class FavoriteVideoDetailsPage extends StatelessWidget {
  const FavoriteVideoDetailsPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('FavoriteVideoDetailsPage'),
      ),
    );
  }


  
}
